{
  "name": "boomi-xcel",
  "version": "2.2.1.1",
  "private": true,
  "description": "Browser extension enhancing the Boomi Platform web UI",
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "node scripts/build.js --watch",
    "release": "node scripts/build.js --release",
    "releaseall": "node scripts/build.js --releaseall"
  },
  "devDependencies": {
    "archiver": "^8.0.0",
    "esbuild": "^0.28.1"
  }
}
